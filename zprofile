# this file is just here so that sublime can find it
# http://sublimelinter.readthedocs.io/en/latest/troubleshooting.html#adjusting-shell-startup-files

# add NVM
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm